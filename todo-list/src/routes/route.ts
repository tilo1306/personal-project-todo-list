import { Router } from 'express'

const router = Router()

router.post('/register')
router.post('/login')
router.get('/task/:id')
router.post('/task/:id')
router.put('/task/:id')
router.delete('task/:id')
